>`DispatcherServlet`通过`handlerMapping`查找对应的`handler`和拦截器`Interceptors`

## handlerMapping

### 核心作用 翻译URL

查找url的映射关系，返回结果

### 返回结果 `HandlerExecutionChain`

返回的结果为`HandlerExcutionChain`，包括：

- Handler（处理器）：具体的Controller方法
- Interceptors（拦截器）：针对当前URL的拦截方法