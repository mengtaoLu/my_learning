---
title: 学习指南
sidebar_label: 学习指南
hide_table_of_contents: true
---

import React, { useState } from 'react';
import { 
  Rocket, 
  Database, 
  ShieldCheck, 
  Cpu, 
  Layout, 
  Terminal, 
  Cloud,
  ChevronRight,
  BookOpen,
  AlertCircle,
  Lightbulb,
  CheckCircle2
} from 'lucide-react';

export const Roadmap = () => {
  const roadmapData = [
  {
    id: 'foundation',
    title: '第一阶段：核心机制 (Magic)',
    icon: <Rocket className="w-6 h-6" />,
    color: 'bg-blue-500',
    description: '摆脱 XML 和繁琐配置，理解 Spring Boot 的“自动魔力”。',
    topics: [
      '自动配置原理 (@EnableAutoConfiguration)',
      '条件注解 (@ConditionalOnClass/Property)',
      'Starter POMs 依赖管理机制',
      'YAML 层次化配置与多环境 Profile',
      '自定义 @ConfigurationProperties 绑定'
    ],
    bridge: '从 Spring MVC 的手动 Bean 配置转向“约定优于配置”。',
    tips: '重点研究 spring.factories 文件，它是自动配置的入口。'
  },
  {
    id: 'data',
    title: '第二阶段：持久化层 (Persistence)',
    icon: <Database className="w-6 h-6" />,
    color: 'bg-emerald-500',
    description: '如何高效地与数据库通信，并保证事务安全。',
    topics: [
      'Spring Data JPA 核心概念与 Repository 模式',
      'MyBatis Plus 快速集成与 CRUD 增强',
      '声明式事务管理 (@Transactional) 深度解析',
      'HikariCP 与 Druid 连接池配置',
      'Flyway/Liquibase 数据库版本管理'
    ],
    bridge: '在 MVC 基础上，学习如何用 Service 层优雅地封装 Repository 操作。',
    tips: '一定要理解事务失效的场景（如同类调用、非 public 方法）。'
  },
  {
    id: 'web-advanced',
    title: '第三阶段：Web 增强 (Advanced)',
    icon: <Layout className="w-6 h-6" />,
    color: 'bg-purple-500',
    description: '在 Spring MVC 基础上，构建健壮的 RESTful API。',
    topics: [
      '参数校验 (Validation) 与自定义注解',
      '统一响应体封装 (ResultBody)',
      '全局异常拦截器 (RestControllerAdvice)',
      'Jackson 序列化定制与枚举处理',
      '异步任务 (Async) 与定时任务 (Scheduled)'
    ],
    bridge: '你已经学过 Interceptor，这里学习如何用 AOP 做更通用的切面逻辑。',
    tips: '不要在拦截器里写复杂的业务逻辑，尽量保持其轻量化。'
  },
  {
    id: 'security',
    title: '第四阶段：安全攻防 (Security)',
    icon: <ShieldCheck className="w-6 h-6" />,
    color: 'bg-red-500',
    description: '保护你的接口，处理登录验证与权限控制。',
    topics: [
      'Spring Security 核心过滤器链',
      '基于 JWT 的无状态认证方案',
      'RBAC 权限模型 (用户-角色-权限)',
      '方法级权限控制 (@PreAuthorize)',
      '跨域 (CORS) 与 CSRF 深度防御'
    ],
    bridge: '不再靠 Session 维护状态，学习分布式环境下的身份校验。',
    tips: '先学概念，再看代码。Spring Security 的过滤器链非常复杂。'
  },
  {
    id: 'cache',
    title: '第五阶段：中间件 (Middleware)',
    icon: <Cpu className="w-6 h-6" />,
    color: 'bg-orange-500',
    description: '应对高并发，引入缓存与消息系统。',
    topics: [
      'Spring Cache 抽象与 Redis 落地',
      'Redisson 实现分布式锁',
      'RabbitMQ 或 Kafka 异步解耦',
      'Elasticsearch 实现高性能搜索'
    ],
    bridge: '这是从单体应用迈向高性能系统的必经之路。',
    tips: '注意缓存穿透、击穿、雪崩这“三座大山”。'
  },
  {
    id: 'ops',
    title: '第六阶段：监控与运维 (Ops)',
    icon: <Terminal className="w-6 h-6" />,
    color: 'bg-slate-600',
    description: '让你的应用在生产环境下“透明化”。',
    topics: [
      'Actuator 端点监控 (Health, Metrics)',
      'Prometheus + Grafana 仪表盘',
      'Logback 日志滚动配置',
      '单元测试 (JUnit 5 + Mockito)',
      '应用分层测试策略'
    ],
    bridge: '确保你写的 MVC 逻辑不仅能跑，还要能量化和被监控。',
    tips: 'Mockito 是测试中模拟 Service 调用的神器，必须掌握。'
  },
  {
    id: 'cloud',
    title: '第七阶段：微服务化 (Cloud)',
    icon: <Cloud className="w-6 h-6" />,
    color: 'bg-indigo-500',
    description: '当应用变大时，如何拆分与协同。',
    topics: [
      'Spring Cloud Alibaba 生态',
      'Nacos 注册中心与配置中心',
      'Sentinel 流量防卫兵',
      'Feign 声明式客户端调用',
      'Docker/K8s 容器化部署'
    ],
    bridge: '将多个 Spring Boot 实例连接成一个完整的微服务群。',
    tips: '微服务不是银弹，先做深单体应用再考虑拆分。'
  }
];

  const [activeModule, setActiveModule] = useState(roadmapData[0]);

  return (
    <div className="bg-gray-50 flex flex-col md:flex-row rounded-lg overflow-hidden border border-gray-200" style={{minHeight: '600px'}}>
      {/* 左侧侧边栏导航 */}
      <nav className="w-full md:w-80 bg-white border-r border-gray-200 overflow-y-auto">
        <div className="p-6">
          <h1 className="text-xl font-bold text-gray-800 flex items-center gap-2">
            <Rocket className="text-blue-600" />
            进阶之路
          </h1>
        </div>
        <div className="space-y-1 px-3 pb-3">
          {roadmapData.map((module) => (
            <button
              key={module.id}
              onClick={() => setActiveModule(module)}
              className={`w-full flex items-center gap-3 px-4 py-3 rounded-lg text-left transition-all ${
                activeModule.id === module.id 
                ? `${module.color} text-white shadow-md` 
                : 'text-gray-600 hover:bg-gray-100'
              }`}
            >
              {module.icon}
              <span className="font-medium text-sm">{module.title.split('：')[1]}</span>
              {activeModule.id === module.id && <ChevronRight className="ml-auto w-4 h-4" />}
            </button>
          ))}
        </div>
      </nav>

      {/* 右侧内容区 */}
      <main className="flex-1 p-6 md:p-8 overflow-y-auto">
        <div className="max-w-3xl mx-auto">
          {/* 模块标题 */}
          <div className="flex items-center gap-4 mb-8">
            <div className={`p-4 rounded-2xl ${activeModule.color} text-white shadow-lg`}>
              {activeModule.icon}
            </div>
            <div>
              <h2 className="text-2xl font-bold text-gray-900">{activeModule.title}</h2>
              <p className="text-gray-500 mt-1">{activeModule.description}</p>
            </div>
          </div>

          {/* 核心知识点卡片 */}
          <div className="bg-white rounded-2xl p-6 shadow-sm border border-gray-100 mb-6">
            <h3 className="text-lg font-semibold text-gray-800 mb-4 flex items-center gap-2">
              <BookOpen className="w-5 h-5 text-blue-500" />
              核心知识清单
            </h3>
            <ul className="grid grid-cols-1 gap-3">
              {activeModule.topics.map((topic, idx) => (
                <li key={idx} className="flex items-start gap-3 p-3 rounded-lg bg-gray-50 hover:bg-blue-50 transition-colors">
                  <CheckCircle2 className="w-5 h-5 text-blue-400 mt-0.5" />
                  <span className="text-gray-700">{topic}</span>
                </li>
              ))}
            </ul>
          </div>

          {/* 关联与避坑 */}
          <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div className="bg-blue-50 border border-blue-100 rounded-xl p-6">
              <h4 className="flex items-center gap-2 text-blue-800 font-bold mb-3">
                <Lightbulb className="w-5 h-5" />
                MVC 衔接点
              </h4>
              <p className="text-blue-700 text-sm leading-relaxed italic">
                "{activeModule.bridge}"
              </p>
            </div>
            <div className="bg-amber-50 border border-amber-100 rounded-xl p-6">
              <h4 className="flex items-center gap-2 text-amber-800 font-bold mb-3">
                <AlertCircle className="w-5 h-5" />
                专家建议 (Tips)
              </h4>
              <p className="text-amber-700 text-sm leading-relaxed">
                {activeModule.tips}
              </p>
            </div>
          </div>

          {/* 进度提示 */}
          <div className="mt-8 pt-6 border-t border-gray-200">
            <div className="flex justify-between items-center text-sm text-gray-500">
              <span>当前模块: {activeModule.title}</span>
              <span>建议学习周期: 1-2 周</span>
            </div>
            <div className="w-full bg-gray-200 h-2 rounded-full mt-4">
              <div 
                className={`h-full rounded-full transition-all duration-500 ${activeModule.color}`}
                style={{ width: `${((roadmapData.indexOf(activeModule) + 1) / roadmapData.length) * 100}%` }}
              ></div>
            </div>
          </div>
        </div>
      </main>
    </div>
  );
};

<Roadmap />