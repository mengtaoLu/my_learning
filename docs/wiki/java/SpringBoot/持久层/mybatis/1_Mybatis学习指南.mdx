---
title: MyBatis学习指南
sidebar_position: 1
hide_table_of_contents: true
---
---

import React, { useState } from 'react';
import { 
  BookOpen, 
  Code, 
  Database, 
  Zap, 
  Layers, 
  CheckCircle, 
  ChevronRight, 
  FileCode, 
  Settings,
  Cpu
} from 'lucide-react';

export const topics = [
  {
    id: 'intro',
    title: 'MyBatis 简介',
    icon: <BookOpen size={20} />,
    content: {
      description: 'MyBatis 是一款优秀的持久层框架，它支持自定义 SQL、存储过程以及高级映射。',
      points: [
        '半自动 ORM：相比 JPA 的全自动，MyBatis 需要手写 SQL，换取更高的灵活性。',
        '消除 JDBC 冗余：自动处理参数设置和结果集封装。',
        'SQL 与代码解耦：通过 XML 或注解将 SQL 逻辑从 Java 代码中分离。',
        '简单易学：基于原生 SQL，只要会 SQL 就能上手。'
      ],
      codeTitle: 'MyBatis 核心架构',
      code: `SqlSessionFactoryBuilder -> SqlSessionFactory -> SqlSession -> Executor -> Mapped Statement`
    }
  },
  {
    id: 'config',
    title: '核心配置',
    icon: <Settings size={20} />,
    content: {
      description: 'MyBatis 的全局配置文件（mybatis-config.xml）控制着框架的行为。',
      points: [
        'environments: 配置数据库连接环境（开发、生产等）。',
        'mappers: 注册 SQL 映射文件或接口。',
        'typeAliases: 为长类名设置别名，简化 XML 编写。',
        'settings: 开启日志、驼峰命名转换等全局开关。'
      ],
      codeTitle: 'mybatis-config.xml 示例',
      code: `<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
  <settings>
    <!-- 开启驼峰命名自动映射 -->
    <setting name="mapUnderscoreToCamelCase" value="true"/>
  </settings>
  
  <typeAliases>
    <package name="com.example.entity"/>
  </typeAliases>
</configuration>`
    }
  },
  {
    id: 'mapper',
    title: 'Mapper 映射',
    icon: <FileCode size={20} />,
    content: {
      description: 'Mapper 是 MyBatis 的核心，定义了 Java 接口与 SQL 语句的对应关系。',
      points: [
        'Namespace: 必须与 Mapper 接口的全路径一致。',
        'ID: 必须与接口中的方法名一致。',
        'resultMap: 用于解决数据库列名与 Java 属性名不一致的复杂映射。',
        '参数传递: 使用 @Param 注解或 Map/对象传递参数。'
      ],
      codeTitle: 'UserMapper.xml 示例',
      code: `<mapper namespace="com.example.mapper.UserMapper">
  <select id="getUserById" resultType="User">
    SELECT * FROM users WHERE id = #{id}
  </select>
  
  <insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO users (username, password) 
    VALUES (#{username}, #{password})
  </insert>
</mapper>`
    }
  },
  {
    id: 'dynamic-sql',
    title: '动态 SQL',
    icon: <Zap size={20} />,
    content: {
      description: '动态 SQL 是 MyBatis 最强大的特性之一，利用 OGNL 表达式灵活构建 SQL。',
      points: [
        '<if>: 条件判断，避免拼接 "WHERE 1=1"。',
        '<choose>, <when>, <otherwise>: 类似于 Java 的 switch-case。',
        '<where>: 自动处理多余的 AND/OR。',
        '<foreach>: 循环遍历集合，常用于 IN 查询。'
      ],
      codeTitle: '动态 SQL 示例',
      code: `<select id="findActiveUsers" resultType="User">
  SELECT * FROM users
  <where>
    <if test="username != null">
      AND username LIKE #{username}
    </if>
    <if test="status != null">
      AND status = #{status}
    </if>
  </where>
</select>`
    }
  },
  {
    id: 'spring-boot',
    title: 'Spring Boot 集成',
    icon: <Cpu size={20} />,
    content: {
      description: '在现代开发中，MyBatis 通常与 Spring Boot 配合使用，实现零 XML 配置或极简配置。',
      points: [
        '依赖引入: 使用 mybatis-spring-boot-starter。',
        '@MapperScan: 在启动类上扫描 Mapper 接口。',
        'application.yml: 配置数据源和 MyBatis 属性。',
        'Service 层调用: 直接 @Autowired 注入 Mapper。'
      ],
      codeTitle: 'application.yml 配置',
      code: `mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.model
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # 开启 SQL 日志`
    }
  }
];

export const App = () => {
  const [activeTab, setActiveTab] = useState(topics[0].id);

  const currentTopic = topics.find(t => t.id === activeTab);

  return (
    <div className="flex bg-slate-50 font-sans text-slate-900 h-[calc(100vh-4rem)]">
      {/* Sidebar */}
      <aside className="w-64 bg-white border-r border-slate-200 flex flex-col">
        <div className="p-6 border-bottom border-slate-100">
          <div className="flex items-center gap-2 text-blue-600 font-bold text-xl">
            <Layers className="animate-pulse" />
            <span>MyBatis Guide</span>
          </div>
          <p className="text-xs text-slate-400 mt-1 uppercase tracking-wider font-semibold">Learning Roadmap</p>
        </div>
        
        <nav className="flex-1 overflow-y-auto py-4">
          {topics.map((topic) => (
            <button
              key={topic.id}
              onClick={() => setActiveTab(topic.id)}
              className={`w-full flex items-center gap-3 px-6 py-3 text-sm font-medium transition-all ${
                activeTab === topic.id 
                  ? 'bg-blue-50 text-blue-600 border-r-4 border-blue-600' 
                  : 'text-slate-500 hover:bg-slate-50 hover:text-slate-700'
              }`}
            >
              {topic.icon}
              {topic.title}
              {activeTab === topic.id && <ChevronRight className="ml-auto" size={16} />}
            </button>
          ))}
        </nav>

        <div className="p-4 bg-slate-900 text-slate-400 text-xs text-center">
          © 2024 MyBatis 核心教程
        </div>
      </aside>

      {/* Main Content */}
      <main className="flex-1 overflow-y-auto p-8 lg:p-12">
        <div className="max-w-4xl mx-auto">
          <header className="mb-10">
            <h1 className="text-4xl font-extrabold text-slate-900 mb-4">{currentTopic.title}</h1>
            <p className="text-lg text-slate-600 leading-relaxed">
              {currentTopic.content.description}
            </p>
          </header>

          <div className="grid grid-cols-1 gap-8">
            {/* Points Section */}
            <section className="bg-white rounded-2xl shadow-sm border border-slate-200 p-8">
              <h2 className="text-xl font-bold mb-6 flex items-center gap-2">
                <CheckCircle className="text-green-500" size={24} />
                关键知识点
              </h2>
              <ul className="space-y-4">
                {currentTopic.content.points.map((point, idx) => (
                  <li key={idx} className="flex items-start gap-3 text-slate-700">
                    <span className="w-6 h-6 rounded-full bg-blue-100 text-blue-600 flex items-center justify-center text-xs font-bold shrink-0 mt-0.5">
                      {idx + 1}
                    </span>
                    <span>{point}</span>
                  </li>
                ))}
              </ul>
            </section>

            {/* Code Section */}
            <section className="bg-slate-900 rounded-2xl shadow-xl overflow-hidden">
              <div className="px-6 py-3 border-b border-slate-800 flex justify-between items-center bg-slate-800/50">
                <div className="flex items-center gap-2 text-slate-300 text-sm font-mono">
                  <Code size={16} />
                  {currentTopic.content.codeTitle}
                </div>
                <div className="flex gap-1.5">
                  <div className="w-3 h-3 rounded-full bg-red-500/80"></div>
                  <div className="w-3 h-3 rounded-full bg-yellow-500/80"></div>
                  <div className="w-3 h-3 rounded-full bg-green-500/80"></div>
                </div>
              </div>
              <div className="p-6 overflow-x-auto">
                <pre className="text-sm font-mono text-blue-100 leading-relaxed">
                  <code>{currentTopic.content.code}</code>
                </pre>
              </div>
            </section>

            {/* Footer Tip */}
            <div className="bg-blue-600 rounded-2xl p-8 text-white flex items-center gap-6">
              <div className="bg-white/20 p-4 rounded-xl">
                <Database size={40} />
              </div>
              <div>
                <h3 className="text-xl font-bold mb-1">进阶提示</h3>
                <p className="opacity-90">
                  掌握了本节内容后，建议通过实践一个简单的 CRUD 项目来巩固。你可以结合之前的 OrbStack 部署 PostgreSQL 来测试你的 SQL 映射！
                </p>
              </div>
            </div>
          </div>
        </div>
      </main>
    </div>
  );
};

<App />