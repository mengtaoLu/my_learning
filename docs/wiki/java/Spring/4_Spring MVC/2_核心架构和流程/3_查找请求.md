> `DispatcherServlet` 通过 `HandlerMapping` 查找对应的 `handler` 和拦截器 `Interceptors`

## HandlerMapping

### 核心作用 翻译 URL

查找 url 的映射关系，返回结果

### 返回结果 `HandlerExecutionChain`

返回的结果为 `HandlerExecutionChain`，包括：

- Handler（处理器）：具体的 Controller 方法
- Interceptors（拦截器）：针对当前 URL 的拦截方法